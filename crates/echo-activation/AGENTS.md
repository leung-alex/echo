# Activation Ownership

This crate owns the small Echo activation envelope and canonical
`--echo-activate` flag. Read `docs/architecture/overview.md`; do not add
feature persistence or UI behavior here.
