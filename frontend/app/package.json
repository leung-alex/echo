{
  "name": "@echo/recall-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "dev": "vite --host 127.0.0.1 --port 5174",
    "test": "tsc --noEmit && pnpm --dir ../.. test:ui",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tauri-apps/api": "2.11.1"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "vite": "7.0.6"
  }
}
