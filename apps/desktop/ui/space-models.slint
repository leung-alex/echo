export struct SpaceVm {
    key: string, title: string, icon-key: string, accent: color,
    count: string, system: bool, selected: bool,
}
export struct ActionVm { key: string, label: string, detail: string, danger: bool, enabled: bool }
