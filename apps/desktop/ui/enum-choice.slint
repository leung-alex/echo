import { EchoSelect } from "echo-select.slint";

// Preserve the existing enum adapter contract; loading a value is not a user edit.
export component EnumChoice inherits EchoSelect {
    callback selected(string);
    selection-changed(index) => { root.selected(root.model[index]); }
}
